// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id            Int      @id @default(autoincrement())
  uuid          String   @unique @default(uuid())
  name          String   @unique
  email         String   @unique
  password      String
  phone         Int
  img           String?
  role          String?   @default("user")
  gender        String
  refresh_token String? @db.Text
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  cart      Cart[]
  order       Order[]
}

model Product {
  id         Int     @id @default(autoincrement())
  uuid       String  @unique @default(uuid())
  name       String
  img        Json
  categories Json
  detail     String
  desc       String
  price      Int
  rating     Int?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  stock      Stock[]
  cart Cart[]
}
model Cart {
  id         Int     @id @default(autoincrement())
  uuid       String  @unique @default(uuid())
  user_id    String  
  product_id   String
  stock_id   String
  order_id   String?
  quantity   Int
  price      Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  product      Product @relation(fields: [product_id], references: [uuid])
  stock      Stock @relation(fields: [stock_id], references: [uuid])
  user       User @relation(fields: [user_id], references: [uuid])
  order       Order? @relation(fields: [order_id], references: [uuid])
}
model Stock {
  id         Int     @id @default(autoincrement())
  uuid       String  @unique @default(uuid())
  product_id String
  size       String
  color      String
  stock      Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  product    Product @relation(fields: [product_id], references: [uuid])
  cart        Cart[]
}
model Order {
  id         Int     @id @default(autoincrement())
  uuid       String  @unique @default(uuid())
  user_id    String
  totalPrice Int
  totalQuantity Int
  name       String @db.Text
  country    String @db.Text
  city       String @db.Text
  address    String @db.Text
  postal_code    Int
  status       String?   @default("proccess")
  user       User @relation(fields: [user_id], references: [uuid])
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  cart    Cart[]
}
model Slide {
  id         Int     @id @default(autoincrement())
  uuid       String  @unique @default(uuid())
  img         String
  title       String
  detail      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
model Category_preview {
  id         Int     @id @default(autoincrement())
  uuid       String  @unique @default(uuid())
  img         String
  title       String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}